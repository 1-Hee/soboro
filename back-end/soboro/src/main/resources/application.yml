# Local 설정
server:
  port: 8080

spring:
  profiles:
    active: server

AdminMail:
  password: qeogczoriszdrykk
  id: soboroservice@gmail.com
mail:
  smtp:
    socketFactory:
      class: javax.net.ssl.SSLSocketFactory
      fallback: 'false'
      port: '465'
    starttls:
      required: 'true'
      enable: 'true'
    port: '465'
    auth: 'true'